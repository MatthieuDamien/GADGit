9h10
schéma et recherches d'interviews pour le 14 décembre

14h
j'ai essayer de commencer l'optimiseur
il faut faire des modifs dans 
- **Le Cerveau (`optimizer.py`)** : Il analyse la situation (Météo MongoDB + taille fichiers) et prend une décision intelligente ("Il nous faut la partition `bigmem`").
- **Les Nerfs (`analysis.py`)** : Il transmet l'information. Il appelle le cerveau pour avoir la décision, puis ordonne aux muscles d'agir avec ces paramètres précis.
- **Les Muscles (`subprocess_runner.py`)** : Il exécute l'action brute. Il ne réfléchit pas, il construit juste la ligne de commande avec les paramètres qu'on lui donne.
mais il faut modifier tout pour accepter un cerveau
et il faut modifier `dispatch_sample_and_mv.py` qui n'accepte que nompi.

