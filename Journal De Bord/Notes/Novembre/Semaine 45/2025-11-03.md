9h10
On ajoute le fait de pouvoir se connecter avec un csv plutôt que de changer les données dans le code. J'ai ajouté un chemin de test. J'ai créer un fichier config à la place de l'ajouter dans `analysis_config_mesobfc.tsv` car ça permet de séparer les fichiers et s'il y a quelque chose qui n'est plus utilisé, on sait pourquoi.

10h
Modification de la base de donnée pour qu'il n'y ai plus d'erreurs de job_list dans analysis_summaries (bug évoqué le [[2025-10-30]])

12h 
la galère de trouver le bon endroit :
faire `cd ..` en boucle
faire work/work/shared/s-neomics