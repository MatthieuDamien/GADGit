arrivée 9h

recherche python -> slurm 
https://fr.python-3.com/?p=43664
https://pyslurmutils.readthedocs.io/en/latest/auth.html#slurm-authentication

Tests pour récupérer l'url
```bash
[umw040ir@login-1 ~]$ ps aux | grep slurmrestd
umw040ir  284407  0.0  0.0  12148  1164 pts/2    S+   11:27   0:00 grep --color=auto slurmrestd
```

``` bash
[umw040ir@login-1 ~]$ netstat -tlnp | grep -E ':(6817|6818|8080|8081)'
(Tous les processus ne peuvent être identifiés, les infos sur les processus
non possédés ne seront pas affichées, vous devez être root pour les voir toutes.)
tcp        0      0 0.0.0.0:6818            0.0.0.0:*               LISTEN      -   
```

```bash
[umw040ir@login-1 ~]$ curl -k https://login-1.mesobfc.fr:6818/slurm/v0.0.39/ping

curl: (35) OpenSSL SSL_connect: SSL_ERROR_SYSCALL in connection to login-1.mesobfc.fr:6818 
```

Port 22 (moi)
```bash
[umw040ir@login-1 ~]$ curl -k http://login-1.mesobfc.fr:22/slurm/v0.0.39/ping
SSH-2.0-OpenSSH_8.0
Invalid SSH identification string.
curl: (56) Recv failure: Connexion ré-initialisée par le correspondant
```

Pour le token
```bash
[umw040ir@login-1 ~]$ scontrol token
Error fetching token
```

```bash
[umw040ir@login-1 ~]$ sacctmgr show user $USER format=user,account
      User    Account 
---------- ---------- 
  umw040ir            
```

Conclusion : 
- **L'API REST SLURM est disponible** sur le port 6818 (le `netstat` le confirme)
- **Le problème SSL** indique que l'API REST nécessite probablement une authentification différente
- **Le token JWT n'est pas configuré** sur ce cluster
Donc il faut trouver un moyen de se connecter en lançant un programme bash avec Python pour se connecter au SSH - spoiler : il faut faire une librairie


ce que je dois faire :
```batch
squeue -a
```
récupère les jobs dans la queue 

avec Emilie :
Pour se connecter sans mdp
```batch
cd ~/.ssh
ssh-keygen -t rsa
```
se connecter sur le meso
ouvrir le fichier ~/.ssh/authorized_keys
nedit ~/.ssh/authorized_keys
copier le contenu de la clé (id_rsa.pub) et la coller dans ~/.ssh/authorized_keys
modif de [[Drawing 2025-09-11.excalidraw]] (14h38)

15h30
Essais de connexion automatique au cluster avec Python en passant par des commandes Bash