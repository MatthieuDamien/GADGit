9h10
Mon téléphone m'a lâché ce matin
J'ai l'impression que Quark aussi, ce que j'ai fait hier ne marche pas, j'ai des erreurs 404 lorsque la base de donnée est complètement vide.

11h30
Votre fonction `watchCollections` contient un gestionnaire d'erreurs (`changeStream.on('error', ...)`) qui gère les erreurs _asynchrones_ qui surviennent pendant la vie du stream. Cependant, si l'appel initial à `.watch()` lève une erreur _synchrone_ (par exemple, si la collection n'existe pas encore ou si une option est invalide), il n'est pas rattrapé et fait planter Node.

La solution est de **protéger le corps de la fonction `watchCollections`** avec un bloc `try...catch` pour empêcher toute erreur lors de la création du stream de faire planter tout le serveur.

11h40
Essais d'hydratation

15h25 
Toujours pas réussi

17h10