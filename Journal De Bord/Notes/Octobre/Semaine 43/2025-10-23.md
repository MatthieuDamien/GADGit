9h05
Pour mes problèmes que mes données disparaissent après 5min alors que j'ai changé à 6h, je pense que soit le redémarrage de l'ordi à marché 
```javascript
mongosh
use quarkdb

// Lancer runquark.py

rs.status()
```

et bien enfaite il faut supprimer la collection et laisser Python en recréer une. Les informations TTL (le temps de délétion d'un élément) est gravé dans la collection, donc on doit supprimer la collection et en créer une nouvelle avec le script.
Maintenant il ne me reste plus que l'hydratation mais c'est un problème difficile

10h45
J'ai remarqué que mes jobs dans sacct ne récupéraient pas le nom de l'utilisateur qui a lancé le job. Donc on va faire ça et rajouter des nouvelles variables :
- **User** : L'utilisateur qui a lancé le job.
- **Start** et **End** : Les heures de début et de fin du job.
- **Submit** : Quand le job a été soumis.
- **Timelimit** : La limite de temps pour le job.
- **WorkDir** : Le répertoire de travail du job.
- **ReqMem** : La mémoire demandée.
- **MaxRSS** : La mémoire maximale utilisée par le job.
Pour le débug ça sera plus simple sans avoir à passer par slurm

Maintenant il faut passer à améliorer tout le code :,)